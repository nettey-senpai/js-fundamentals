const id = 1;
const title = "Product 1";
const rating = 5;

// Only possible when object key names are the same as property names passed 
const product = {
  id,
  title,
  rating,
};

console.log(product);
